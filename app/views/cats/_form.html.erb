<%= form_for @cat do |f| %>
  <p>name: <%= f.text_field :name %></p>
  <p>breed: <%= f.text_field :breed %></p>
  <p>select_owner: <%= f.collection_select(:user_id, User.all, :id, :username) %></p>

  <p><%= f.submit %></p>
<% end %>